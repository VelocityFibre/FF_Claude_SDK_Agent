# ==============================================================================
# Agent Workforce Environment Variables
# ==============================================================================
#
# This file documents ALL environment variables used across the agent system.
# Copy this file to .env and fill in your actual values.
#
# IMPORTANT: Never commit .env to version control! It's in .gitignore.
#
# ==============================================================================

# ------------------------------------------------------------------------------
# REQUIRED FOR ALL AGENTS
# ------------------------------------------------------------------------------

# Anthropic API key for Claude
# Get yours at: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# ------------------------------------------------------------------------------
# VF SERVER OPERATIONS
# ------------------------------------------------------------------------------

# Velocity Fibre server credentials (Tailscale access)
# Purpose: Internal operations, BOSS integration, QField sync
# Production paths: /srv/data/apps/, /srv/scripts/cron/
VF_SERVER_HOST=100.96.203.105
VF_SERVER_USER=louis
# VF_SERVER_PASSWORD is optional - leave unset to use SSH key authentication
# VF_SERVER_PASSWORD=your-password-here

# Alternative hostnames
# VF_SERVER_TAILSCALE_HOSTNAME=velo-server
# VF_SERVER_LAN_IP=192.168.1.150

# ------------------------------------------------------------------------------
# VPS MONITOR AGENT
# ------------------------------------------------------------------------------

# VPS hostname or IP address to monitor
# Supported servers:
#   - Hostinger VPS: srv1092611.hstgr.cloud (72.60.17.245) - Public FibreFlow
#   - VF Server: velo-server (100.96.203.105) - Internal operations
VPS_HOSTNAME=your-vps-hostname.com

# SSH user for VPS access (optional, defaults to 'root')
# VPS_SSH_USER=root

# Path to SSH private key (optional, uses default SSH config if not set)
# VPS_SSH_KEY_PATH=~/.ssh/id_ed25519

# ------------------------------------------------------------------------------
# NEON DATABASE AGENT
# ------------------------------------------------------------------------------

# Neon PostgreSQL connection string
# Format: postgresql://user:password@host/database?sslmode=require
# Get from: https://console.neon.tech/app/projects
NEON_DATABASE_URL=postgresql://user:password@ep-cool-name-123456.us-east-2.aws.neon.tech/neondb?sslmode=require

# ------------------------------------------------------------------------------
# CONVEX DATABASE AGENT
# ------------------------------------------------------------------------------

# Convex deployment URL
# Get from: https://dashboard.convex.dev/deployment/settings
CONVEX_URL=https://your-deployment-name.convex.cloud

# Sync authentication key (optional, for syncing data)
# Get from Convex dashboard if using sync features
# SYNC_AUTH_KEY=your-sync-key-here

# ------------------------------------------------------------------------------
# SHAREPOINT INTEGRATION
# ------------------------------------------------------------------------------

# Azure AD application credentials for SharePoint access
# Get from: https://portal.azure.com/#view/Microsoft_AAD_IAM/ActiveDirectoryMenuBlade/~/RegisteredApps
SHAREPOINT_TENANT_ID=your-tenant-id
SHAREPOINT_CLIENT_ID=your-client-id
SHAREPOINT_CLIENT_SECRET=your-client-secret

# SharePoint site information
SHAREPOINT_SITE_PATH=/sites/Your_Site_Name
SHAREPOINT_SITE_URL=https://yourcompany.sharepoint.com/sites/Your_Site_Name
SHAREPOINT_FILE_URL=https://yourcompany.sharepoint.com/:x:/s/Your_Site_Name/your-file-id

# ------------------------------------------------------------------------------
# CONTRACTOR & PROJECT AGENTS
# ------------------------------------------------------------------------------

# These agents use Convex, so they require:
# - CONVEX_URL (see above)
# Both agents share the same Convex deployment

# No additional variables needed for Contractor Agent
# No additional variables needed for Project Agent

# ------------------------------------------------------------------------------
# TESTING & DEVELOPMENT
# ------------------------------------------------------------------------------

# Set to 'true' to enable debug logging
# DEBUG=false

# Set to 'true' to run in test mode (uses mock data)
# TEST_MODE=false

# ------------------------------------------------------------------------------
# USAGE NOTES
# ------------------------------------------------------------------------------
#
# 1. Required Variables:
#    - All agents need: ANTHROPIC_API_KEY
#    - VPS Monitor: VPS_HOSTNAME
#    - Neon Database: NEON_DATABASE_URL
#    - Convex/Contractor/Project: CONVEX_URL
#
# 2. Optional Variables:
#    - VPS_SSH_USER, VPS_SSH_KEY_PATH (for VPS Monitor)
#    - SYNC_AUTH_KEY (for Convex sync features)
#    - DEBUG, TEST_MODE (for development)
#
# 3. Loading Environment Variables:
#    The agents will automatically load from .env if it exists.
#    You can also set variables in your shell:
#      export ANTHROPIC_API_KEY="sk-ant-api03-..."
#
# 4. Validation:
#    Run this to check your environment setup:
#      python shared/config.py
#
# 5. Security:
#    - Never commit .env to git
#    - Keep your API keys secret
#    - Use separate keys for dev/prod
#    - Rotate keys periodically
#
# ==============================================================================
