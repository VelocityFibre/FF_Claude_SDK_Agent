# Frontend Environment Variables (Next.js)
# Add these to your .env.local file in VF/Apps/FF_React/

# Required: Python backend URL
# Local development:
AGENT_BACKEND_URL=http://localhost:8000

# Production (after Railway deployment):
# AGENT_BACKEND_URL=https://your-app.railway.app

# Optional: API Key (must match backend)
# Only needed if you set AGENT_API_KEY in backend
AGENT_API_KEY=your-random-api-key-here
